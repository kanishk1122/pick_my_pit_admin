"use client";

import { useState } from "react";
import { useTheme } from "@/context/ThemeContext";

export default function Navbar() {
  return (
    <nav className="bg-zinc-800 border-b border-zinc-700 h-16 flex items-center px-4">
      {/* Navbar content */}
    </nav>
  );
}
